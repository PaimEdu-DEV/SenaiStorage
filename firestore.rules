rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra temporaria para o prototipo sem autenticacao.
    // Trocar por regras por perfil quando o login for implementado.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
